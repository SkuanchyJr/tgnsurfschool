// Supabase has been removed. Database access is now via src/lib/db.ts (PostgreSQL).
// This file is kept for import compatibility — all exports are stubs.
export const supabase = null as any;
export const supabaseAdmin = null as any;
